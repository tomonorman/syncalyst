<div class="row">
  <% @meeting.tasks.each do |task| %>
    <div class="col-6">
      <div class="task-cards-complete d-flex mx-3 my-3 justify-content-between  align-items-center m-1">
        <div class="avatar d-flex justify-content-start align-items-center">
          <%= cl_image_tag "#{task.user.photo.key}", alt: "task-user-avatar", class: "avatar mx-1" %>
          <%= task.user.nickname %>
        </div>
        <div class="task-info">
          <%= task.description %>
        </div>
      </div>
    </div>
  <% end %>
</div>